*** Begin Patch
*** Update File: android/src/main/java/com/swmansion/rnscreens/ScreenWindowTraits.kt
@@
-    internal fun setColor(
+    internal fun setColor(
         screen: Screen,
         activity: Activity?,
         context: ReactContext?,
@@
-        if (activity == null || context == null) {
-            return
-        }
+        if (activity == null || context == null) {
+            return
+        }
@@
-        if (defaultStatusBarColor == null) {
-            defaultStatusBarColor = activity.window.statusBarColor
-        }
+        if (Build.VERSION.SDK_INT >= 35) {
+            return
+        }
+
+        if (defaultStatusBarColor == null) {
+            defaultStatusBarColor = activity.window.statusBarColor
+        }
@@
-    internal fun setNavigationBarColor(
+    internal fun setNavigationBarColor(
         screen: Screen,
         activity: Activity?,
     ) {
@@
-        val window = activity.window
-
-        val screenForNavBarColor = findScreenForTrait(screen, WindowTraits.NAVIGATION_BAR_COLOR)
-        val color = screenForNavBarColor?.navigationBarColor ?: window.navigationBarColor
-
-        UiThreadUtil.runOnUiThread {
-            WindowInsetsControllerCompat(window, window.decorView).isAppearanceLightNavigationBars =
-                isColorLight(color)
-        }
-        window.navigationBarColor = color
+        val window = activity.window
+
+        val screenForNavBarColor = findScreenForTrait(screen, WindowTraits.NAVIGATION_BAR_COLOR)
+        val color = screenForNavBarColor?.navigationBarColor ?: run {
+            if (Build.VERSION.SDK_INT >= 35) {
+                null
+            } else {
+                window.navigationBarColor
+            }
+        }
+
+        UiThreadUtil.runOnUiThread {
+            if (color != null) {
+                WindowInsetsControllerCompat(window, window.decorView).isAppearanceLightNavigationBars =
+                    isColorLight(color)
+            }
+        }
+
+        if (color != null && Build.VERSION.SDK_INT < 35) {
+            window.navigationBarColor = color
+        }
*** End Patch