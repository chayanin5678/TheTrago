# Environment Variables for OTP Backend API
# คัดลอกไฟล์นี้เป็น .env และแก้ไขค่าตามจริง

# Server Configuration
PORT=3000
NODE_ENV=development

# Email Configuration (Gmail Example)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# For Gmail:
# 1. เปิด 2-Factor Authentication
# 2. สร้าง App Password ใน Google Account Settings
# 3. ใช้ App Password แทน password ปกติ

# For other email providers:
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false

# API Configuration
API_BASE_URL=https://your-backend-api.com

# Security
JWT_SECRET=your-super-secret-jwt-key-here
SESSION_SECRET=your-session-secret-here

# Rate Limiting
OTP_RATE_LIMIT_WINDOW=15
OTP_RATE_LIMIT_MAX=5

# OTP Configuration
OTP_EXPIRY_MINUTES=5
OTP_MAX_ATTEMPTS=3

# Database (Optional - if using database instead of in-memory)
# DATABASE_URL=mongodb://localhost:27017/thetrago
# REDIS_URL=redis://localhost:6379
